# Session Log — 2026-08-03

**Type:** framework maintenance (audit → correct → improve)
**Skills used:** session-mlt (close)

## Accomplished

1. **Full framework audit** (3 parallel deep reviews + live URL verification) against the original generation prompt — ~35 defects found across deploy machinery, `.cursorrules`, skills, standards, curricula, and repo hygiene.
2. **All defects fixed:**
   - `deploy-basic.sh` rewritten (token substitution, working `--force`/`--update`) — thin-client deploy was functionally broken
   - `bootstrap.sh` hardened (no more source-identity fallback, full scaffold incl. labs/tutorials/drills, `.quick` populated)
   - `.cursorrules`: phantom `@x-director` removed, fat-client `.ai.mlt/` resolution fixed, "MLT Professor OS" leftovers renamed, 3 missing placeholder docs added
   - `SKILL_DEPENDENCIES.md` graph redrawn to match gate table; `mlt-sources` row added; registry verbs synced
   - Lab venvs anchored inside `.training.mlt/` everywhere (memory boundary)
   - Standards: VRAM table corrected, `hf cache ls/rm` replaces invented CLI, program-spec Level enum + prerequisites aligned with reality
   - Curricula: local-first fallbacks (<3B models, single-GPU, opt-in cloud) in 4 programs; dead links replaced (Open LLM Leaderboard, 3 Anthropic URLs verified broken via fetch); stale sources flagged per citation.md
   - `.gitignore`: skeleton now tracked, `.env.*` covered, lab venvs ignored; `docs/README.md` added
   - `framework-verify.sh` upgraded: registry sync, catalog sync, template/skeleton parity, script syntax, fenced-code-aware relative link check
3. **Drill backlog closed:** 30 lab templates generated (33/33 drills in case-library.md now have templates), structure-checked and spot-verified.

## Verification

- `bash scripts/framework-verify.sh` → 0 errors, 0 warnings (also on simulated fresh clone)
- Deploy flow tested end-to-end in `tmp/` (deploy, `--update`, `--force`), then cleaned up

## Left incomplete

- Nothing. Awaiting user review of the working tree.

## Key decisions

- Fat-client contract = full `.cursorrules`; thin-client = abridged template with pointer to `$TRAINER_MLT_SOURCE/.cursorrules`
- `.training.mlt/` skeleton is tracked in the framework repo (it vanished on fresh clones when ignored)
- Program Level enum allows bridging labels ("Intermediate-Advanced")
