# Framework Audit — pilo.trainer.mlt (self-hosted source)

**What:** Structural audit verifying clarity-contract adoption (per `.work.mlt/prompts/adopt-framework-improvements.md`) plus drift sweep, run per `.work.mlt/prompts/framework-audit.md`.
**Status:** Complete · 2026-08-14 · **Framework:** mlt (self-hosted source repo `/mnt/work/Projects/.ai.mlt`)
**Needs:** review the proposed fixes at the end — none applied.

## Scope

Phases 1–2 of `adopt-framework-improvements.md` were executed and verified before this audit. Checks 1–8 below follow `framework-audit.md`. Evidence is repo-relative `path:L<n>`.

## Check results

### 1. Registry congruence — PASS (1 observation)

- 18 `skills/mlt-*/` directories == skills listed in `.cursorrules` skills table (`.cursorrules:108-127`) == `skills/README.md` registry. Diffed sorted lists: no delta. (confirmed)
- `skills/SKILL_DEPENDENCIES.md` gate graph omits `mlt-director` and `mlt-process-router` (both appear nowhere in that file). Classified **inference**: intentional — the file is a prerequisite gate graph for the learner pipeline, and both skills are gate-free meta/read-only routers. Not drift.
- Enforcement: `scripts/framework-verify.sh:91-99` hard-fails on any skill missing from `skills/README.md` or `.cursorrules`.

### 2. Operator handoff adoption — PASS

- Contract section present: `skills/SKILL_DEPENDENCIES.md:48`.
- Reference in every skill: `> **Close:**` line in all 18 `skills/<id>/skill.md` (grep count 18/18). (confirmed)
- Chat report templates close with Form A/B:
  - `skills/mlt-session/skill.md` — 5 templates (start, status, context, close, commit) each end with `**Next step:**` or `Next: …`.
  - `skills/mlt-process-router/skill.md` — response format + both worked examples end with `Next: …`.
  - `skills/mlt-program-standard/skill.md` — list-mode render ends with `Next: …`.
  - `skills/mlt-review/skill.md` — status template already ended with `Next: …` (pre-existing, unchanged).
- `.cursorrules` synced: closing requirement at `.cursorrules:201` (Verification & Communication).
- Verifier wired: `scripts/framework-verify.sh:58-60,75-78` hard-fails on missing section/reference. Run: 0 errors. (confirmed)
- Enforcement gap: none — this repo has a skill verifier and it is wired.

### 3. Document clarity adoption — PASS

- Contract section present: `skills/SKILL_DEPENDENCIES.md:62`.
- Reference in every doc-generating skill: `> **Docs:**` line in 13/13 (mlt-assess, mlt-bootstrap, mlt-curriculum, mlt-drill, mlt-lab, mlt-mentor, mlt-program-custom, mlt-program-standard, mlt-review, mlt-session, mlt-sources, mlt-tutorial, mlt-update). Excluded as non-doc-generating: mlt-deploy-basic, mlt-deploy-files, mlt-deploy-repo, mlt-director, mlt-process-router. (confirmed)
- `templates/training/` doc templates (HANDOFF, NEXT, PROFILE, UNKNOWNS) all carry a `**What:** / **Status:** / **Needs:**` header and exactly one Next-action close (`## Next action` in HANDOFF/NEXT/PROFILE; one-line `Next action: none — …` in UNKNOWNS, e.g. `templates/training/NEXT.md:3,17`).
- Pristine skeleton copies synced: `.work.mlt/context/PROFILE.md` and `.work.mlt/plans/UNKNOWNS.md` match templates byte-for-byte. `.work.mlt/context/HANDOFF.md` and `.work.mlt/plans/NEXT.md` intentionally untouched — they are filled session memory, not template copies. (confirmed)
- Verifier wired: `scripts/framework-verify.sh:62-66,79-86` (`DOC_SKILLS` set) hard-fails doc-generating skills missing the reference. Run: 0 errors.

### 4. Routing docs — PASS

- `START_HERE.md`, `PROCESS_ROUTER.md`, `README.md`: every `@mlt-*` mention resolves to an existing `skills/<id>/` directory. (confirmed)
- `README.md:123` mentions `mlt-cursorrules-verify` — that is the script `scripts/mlt-cursorrules-verify.sh` in a directory-tree listing, not a skill reference. Not a finding.
- Relative markdown links: all resolve (`scripts/framework-verify.sh` link check, 0 broken).

### 5. Templates ↔ .cursorrules sync — PASS (1 proposal)

- `.cursorrules` placeholder-map paths all exist (`.work.mlt/context/PROFILE.md`, `HANDOFF.md`, `plans/NEXT.md`, `plans/UNKNOWNS.md`, `programs/`, `sessions/`, `sources/`). (confirmed)
- `templates/cursorrules.template` is the intentionally **abridged** thin-client contract (30 lines vs 222); it defers to `$TRAINER_MLT_SOURCE/.cursorrules` for the full ruleset (`templates/cursorrules.template:11`). Structural difference is by design, not drift.
- Observation (**inference**): neither `templates/cursorrules.template` nor `templates/thin-client-section.md` mentions the Operator handoff / Document clarity contracts, so thin-client deploys rely on the source-side `skills/SKILL_DEPENDENCIES.md` pointer. See proposed fix F1.

### 6. Hooks — PASS (N/A)

- No git hook scripts are mandated anywhere in the repo: no `hooks/` references in docs or scripts; `scripts/install.sh` installs no hooks. No stale references. (confirmed)

### 7. Protected surfaces — PASS

- Protected-files list exists only in `.cursorrules` § Protected Files (`.cursorrules:133-145`); there is no protected-paths JSON in this framework, so no dual-list drift is possible. The list covers the framework's high-blast paths (dependency manifests, Dockerfile/compose, `.env*`, catalog trees, `.cursorrules` wholesale replace, git internals). (confirmed)

### 8. Verifiers — PASS

| Verifier | Exit | Output |
|----------|------|--------|
| `scripts/framework-verify.sh` | 0 | PASSED — 0 errors, 2 warnings (expected skeleton divergence: filled HANDOFF/NEXT vs templates) |
| `scripts/mlt-cursorrules-verify.sh .` | 0 | PASS — layout `self`, checkout complete, skeleton complete, 5 REPLACE tokens (operator-filled, expected) |

## Findings summary

| # | Class | Finding |
|---|-------|---------|
| — | confirmed | All 8 checks pass; clarity contracts fully adopted and verifier-enforced. |
| O1 | inference | `mlt-director` / `mlt-process-router` absent from `SKILL_DEPENDENCIES.md` — gate-free by design. |
| O2 | inference | Thin-client templates don't name the clarity contracts; enforcement rides on the source-side pointer. |

## Proposed fixes (NOT applied)

- **F1** (from O2): add one bullet to `templates/cursorrules.template` and `templates/thin-client-section.md` pointing operators at the two contracts in `skills/SKILL_DEPENDENCIES.md`, so thin-client deploys surface the closing requirement without reading the full ruleset. One line per file.
- **F2** (optional, from O1): add a one-line note in `skills/SKILL_DEPENDENCIES.md` stating that gate-free skills (mlt-director, mlt-process-router) are intentionally absent from the gate graph.

## Next action

none — audit complete; apply F1/F2 only with explicit approval.
